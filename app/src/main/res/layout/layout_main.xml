<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_liquid_base">

        <ImageView
            android:id="@+id/liquid_blob_top"
            android:layout_width="360dp"
            android:layout_height="360dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="-80dp"
            android:layout_marginEnd="-70dp"
            android:alpha="0.9"
            android:src="@drawable/bg_liquid_layer_1" />

        <ImageView
            android:id="@+id/liquid_blob_bottom"
            android:layout_width="320dp"
            android:layout_height="320dp"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="-70dp"
            android:layout_marginBottom="120dp"
            android:alpha="0.85"
            android:src="@drawable/bg_liquid_layer_2" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <include layout="@layout/layout_appbar" />

            <FrameLayout
                android:id="@+id/fragment_holder"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />
        </LinearLayout>
    </FrameLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#101826"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_xboard"
        app:itemBackground="@drawable/bg_drawer_selected"
        app:itemIconTint="@color/navigation_item"
        app:itemTextColor="@color/navigation_item"
        app:menu="@menu/main_drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
